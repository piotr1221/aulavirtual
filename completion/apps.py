from django.apps import AppConfig

# clase de configuracion
# para la app completion
class CompletionConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'completion'
